Fase 1: Crear Base Data Resource Común
Crear core/base_data.gd — class_name BaseData extends Resource con: id, display_name, description, icon
ItemData extiende BaseData en vez de Resource — eliminar campos duplicados, renombrar texture → icon
TowerData extiende BaseData en vez de Resource — eliminar campos duplicados
Verificar que todos los .tres cargan correctamente (cuidado con el rename texture → icon)
Archivos: nuevo core/base_data.gd, editar item_data.gd, editar tower_data.gd, verificar todos los .tres

Fase 2: Simplificar Mapeo Resource→Runtime
Refactorizar Relic para guardar referencia a RelicItemData en vez de copiar campos — acceder via data.id, data.rarity, etc. Mantener solo estado runtime: disabled, amount
Refactorizar Consumable igual — referencia a ConsumableItemData, solo estado runtime (signals, uso)
Simplificar GameItem a base mínima: var data: ItemData + campos runtime-only (como hacen las torres con tower.data)
Actualizar consumidores: RelicsManager (relic.data.id), ConsumablesManager, UI de shop, ~40 implementaciones de relics, ~8 de consumables

Fase 3: Consolidar Sistema de Ofertas y Precios
Generalizar ItemOffer para trabajar con cualquier BaseData — en vez de solo ItemData
Fusionar RelicOffersManager y ConsumablesOffersManager en un solo OffersManager con método genérico: create_offers(pool, amount, discount) + filtros como parámetro
Añadir ofertas de torres al mismo sistema — "shop price" (coste de adquirir la carta) vs "build price" (coste dinámico de colocarla, que sigue en TowersPrice)
Corregir bug de mutación de precio — usar diccionario {id: precio_actual} en vez de mutar el Resource
Mantener descuentos separados en Economy (sirven a propósitos de gameplay diferentes)


Fase 4: Preparar para Buffs/Debuffs
Verificar que la arquitectura base permite que futuros BuffData/DebuffData extiendan BaseData
Si son comprables, pueden participar en el sistema de ofertas
No se implementa en este plan — solo validar que la base lo soporta


Verificación
Todos los .tres existentes cargan sin errores en el editor de Godot
Relics aplican/remueven efectos correctamente (phoenix_feather, buda, soya_sauce)
Consumables funcionan (healing_potion, long_shot targeting)
Shop muestra precios correctos con descuentos
Incremento de precio post-compra funciona sin mutar Resources
Flujo de build/upgrade de torres sin cambios
reset_run() inicializa todos los managers correctamente

ecisiones
icon como nombre estándar en BaseData (renombrar texture de ItemData)
GameItem se mantiene como base ligera con var data: ItemData + estado runtime
Shop price ≠ build price para torres: precio de oferta = coste de adquirir carta, build price = coste dinámico de colocar (TowersPrice)
Fusionar sub-managers de ofertas en un solo OffersManager con helpers genéricos
Fix mutación de precios — diccionario en vez de mutar el Resource
Scope: NO incluye implementación de buffs/debuffs, solo prepara la arquitectura
